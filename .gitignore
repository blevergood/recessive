# Generated by the Niobium FHE Application Design AI Assistant (FHEanna). — v0.13.0
# Build tree (DSL path: keep the generated nb_out sources, ignore its build/)
/nb_out/build/
# Per-run homes provisioned by run_test.sh and the two-process demo (keys, ciphertexts).
# Directory-only glob: covers every run mode, and cannot match run_test.sh.
/run_*/
client_home/
server_home/
# Local tooling that must never be committed
.claude/
.agents/
.venv/
__pycache__/
.vscode/
skills-lock.json
# Toolchain replay artifacts
/fhetch_driver_source_*/
# App-specific: this application's trace and profile directories
/score_profile_*/
# Everything under data/ is generated and deterministic (SEED in gen_data.py):
# gen_data.py writes the cohort, weights, bounds and meta; prepare_inputs.py the
# packed batches and the server model; validate_twin.py the reference and twin
# ledgers. A fresh clone regenerates all of it, so none of it is committed.
/data/
